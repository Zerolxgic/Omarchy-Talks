[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "omarchy-talks"
version = "0.1.0"
description = "A thin VoiceBox reader for Omarchy"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
omarchy-talks = "omarchy_talks.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
